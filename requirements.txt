# ==========================
# Core dependencies (cross-platform)
# ==========================
asgiref==3.7.2
attrs==23.2.0
Babel==2.10.3
bidict==0.22.1
blinker==1.7.0

# ==========================
# Linux-only dependencies
# ==========================
bcc==0.1.10; sys_platform == "linux"

# ==========================
# Notes on Brlapi
# ==========================
# Brlapi (Braille accessibility library) cannot be installed via pip.
# If your project requires it, Linux users must install it manually:
#   sudo apt install libbrlapi-dev brltty
# Windows/macOS users can skip Brlapi.

